language: python

python: 2.7

install: 
    - pip install -r requirements.txt
    - pip install pytest-cov
    - pip install coveralls

script: py.test --cov=./auto_login.py

after_success:
    - coveralls
